<%= form_for(@designci) do |f| %>
  <% if @designci.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@designci.errors.count, "error") %> prohibited this designci from being saved:</h2>

      <ul>
      <% @designci.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
