<%= form_for(@interior_color) do |f| %>
  <% if @interior_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interior_color.errors.count, "error") %> prohibited this interior_color from being saved:</h2>

      <ul>
      <% @interior_color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%if I18n.locale == :en %>
    <% Globalize.with_locale(:ar) do %>
      <div class="field">
        <%= f.label :title, "Arabic Title" %><br>
        <%= @interior_color.title %>
      </div>
    <% end %>
    <div class="field">
        <%= f.label :title, "English Title" %><br>
        <%= f.text_field :title %>
      </div>
  <% else %>
    <% Globalize.with_locale(:en) do %>
      <div class="field">
        <%= f.label :title, "English Title" %><br>
        <%= @interior_color.title %>
      </div>
    <% end %>
    <div class="field">
      <%= f.label :title, "Arabic Title" %><br>
      <%= f.text_field :title %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
