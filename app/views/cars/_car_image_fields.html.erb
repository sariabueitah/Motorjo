<div class="nested-fields">
	<% if f.object.image? %>
		<%= image_tag f.object.image.url(:thumb) if f.object.image?  %>
	<% else %>
		<%= f.file_field :image , animated: false %>
	<% end %>
	<%= link_to_remove_association "Remove", f  %>
</div>