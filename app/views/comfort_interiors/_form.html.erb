<%= form_for(@comfort_interior) do |f| %>
  <% if @comfort_interior.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comfort_interior.errors.count, "error") %> prohibited this comfort_interior from being saved:</h2>

      <ul>
      <% @comfort_interior.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% Globalize.with_locale(:en) do %>
    <div class="field">
      <%= f.label :title, "English Title" %><br>
      <%= f.text_field :title %>
    </div>
  <% end %>
  <% Globalize.with_locale(:ar) do %>
    <div class="field">
      <%= f.label :title, "Arabic Title" %><br>
      <%= f.text_field :title %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
