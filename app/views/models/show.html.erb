<p id="notice"><%= notice %></p>

<p>
  <strong>Parent:</strong>
  <%= @model.parent_id %>
</p>

<p>
  <strong>Title:</strong>
  <%= @model.title %>
</p>

<%= link_to 'Edit', edit_model_path(@model) %> |
<%= link_to 'Back', models_path %>
