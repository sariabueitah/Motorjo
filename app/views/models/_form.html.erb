<%= form_for(@model) do |f| %>
  <% if @model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@model.errors.count, "error") %> prohibited this model from being saved:</h2>

      <ul>
      <% @model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :parent_id %>
    <%= collection_select(:model, :parent_id, Make.all, :id, :title) %>
  </div>
  <div class="field">
    <%= f.label :title_ar, "Arabic Title" %><br>
    <%= f.text_field :title_ar %>
  </div>
  <div class="field">
    <%= f.label :title_en, "English Title" %><br>
    <%= f.text_field :title_en %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
